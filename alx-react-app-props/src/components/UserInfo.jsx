/*function UserInfo({ name, email }) {
  return (
    <div>
      <h2>User Info</h2>
      <p>Name: {name}</p>
      <p>Email: {email}</p>
    </div>
  );
}

export default UserInfo;


*/


// src/UserInfo.jsx
import UserDetails from './UserDetails';

function UserInfo() {
  return <UserDetails />;
}

export default UserInfo;
